package eu.darken.butler.editor.core.mode

/**
 * Type of editor mode.
 */
enum class EditorModeType {
    /** Text editing mode - displays content as UTF-8 text with line numbers */
    TEXT,

    /** Hex editing mode - displays content as hexadecimal bytes */
    HEX
}
