<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.INTERNET" />

    <!--    For some reason this affects PackageManager.getSharedLibraries() results, i.e. trichromelibrary-->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <!--    Required to hand a user-selected APK to the system installer via "Open with"; without it the app cannot be authorized as an install source-->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--    Android 11-->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name="eu.darken.butler.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="eu.darken.butler.main.ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Register as a file manager app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.APP_FILES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Handle directory viewing intents -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:mimeType="vnd.android.document/root" />
                <data android:mimeType="vnd.android.document/directory" />
            </intent-filter>

            <!-- "Open with" for files: typed intents -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- "Open with" for files without a type. A filter that declares a mimeType never
                 matches a typeless intent, and file:// intents commonly carry none. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="content" />
                <data android:scheme="file" />
            </intent-filter>

            <!-- Intent filter for handling Explorer workspace shortcuts -->
            <intent-filter>
                <action android:name="eu.darken.butler.EXPLORER_OPEN_PATH" />
                <action android:name="eu.darken.butler.EXPLORER_NEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Intent filter for ACTION_SEND with text (Edit text) -->
            <intent-filter android:label="@string/editor_share_label">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>

            <!-- Intent filter for ACTION_SEND: a single file goes to the arrival dialog, which
                 offers viewing and revealing next to saving -->
            <intent-filter android:label="@string/share_receive_label">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Intent filter for ACTION_SEND_MULTIPLE (Save multiple files as...) -->
            <intent-filter android:label="@string/saver_share_label">
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />

        </provider>

        <!-- Bound service in separate process for file operations on removable storage.
             Process isolation protects main app from vold's SIGINT on storage disconnect. -->
        <service
            android:name="eu.darken.butler.common.files.local.service.IsolatedService"
            android:exported="false"
            android:process=":isolated" />

        <!-- Keeps the process alive while long-running file operations run in the background. -->
        <service
            android:name="eu.darken.butler.main.core.operations.fgs.OperationFgsService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Cancel action for operation notifications. Reached only via explicit PendingIntent. -->
        <receiver
            android:name="eu.darken.butler.main.core.operations.fgs.OperationCancelReceiver"
            android:exported="false" />
    </application>

</manifest>